spring:
  datasource:
    url: ${DATABASE_URL}
    driver-class-name: org.postgresql.Driver

  redis:
    host: ${REDISHOST}
    port: ${REDISPORT}

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false